# Historical validation evidence

Tracked manifests, summaries and small tables here are historical evidence,
not production RTL or Vivado project inputs. Large regenerated `*.rpt` files
are ignored; reproduce them from the corresponding build and validation
scripts. Do not use an old report as the current timing result after RTL or
project membership changes.
