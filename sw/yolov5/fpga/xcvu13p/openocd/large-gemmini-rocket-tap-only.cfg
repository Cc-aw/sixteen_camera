# TAP-only scan for LargeGemminiRocketXCVU13PConfig.
# This does not create a RISC-V target, so it is useful for isolating basic
# JTAG wiring/TDO issues before OpenOCD tries to access the Debug Module.

set _CHIPNAME riscv
jtag newtap $_CHIPNAME cpu -irlen 5
