# Frozen board artifacts

This directory holds explicitly versioned reference inputs used for board
reproduction, not synthesis sources. Each artifact set has its own README.
Newly built ELF, bitstream and capture outputs belong in ignored build/output
directories until intentionally promoted to a named frozen artifact set.
