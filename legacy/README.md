# Legacy RTL

These modules are retained for historical tests and are excluded from the
production whitelist in `build/rtl_manifest.tcl`.

- `rtl/`: retired tensor-sidecar and AXI helper implementations.
- `preprocess/`: retired batch preprocessing path.
