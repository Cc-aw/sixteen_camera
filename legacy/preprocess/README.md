# Retired preprocessing RTL

`batch_preprocess_engine.sv` and `frame_preprocess_accel.sv` are retained for
historical regression tests only. They are not listed in
`build/rtl_manifest.tcl` and must not be added to production synthesis.
